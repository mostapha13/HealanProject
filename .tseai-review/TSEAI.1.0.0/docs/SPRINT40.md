# Sprint 40 — Final Release

All release mechanics are complete. GA promotion is deliberately conditional on live production acceptance. Run `python scripts/ga-readiness.py --require-live`; only exit code 0 authorizes tagging `v1.0.0`.
