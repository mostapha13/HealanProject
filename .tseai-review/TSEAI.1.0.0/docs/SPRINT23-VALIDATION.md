# Sprint 23 Validation

- Deterministic command detector corpus.
- Saved Filter/Alert ownership preserved by existing services.
- Permission parity with REST (`Filter.Save`, `Alert.Create`).
- Anonymous Chat persistence fail-closed.
- Current unsaved filter → alert requires both permissions.
- Chat asset operations release daily question quota.
- Negative routing cases retained to prevent over-capture.
- .NET runtime smoke project: `TSEAI.ChatFilterAssets.SmokeTests`.
